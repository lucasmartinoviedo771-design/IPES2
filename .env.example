# Copiar como .env y ajustar valores reales en cada entorno
DEBUG=True
SECRET_KEY=dev-insecure-key-change-me

# Producción: poné tu(s) dominio(s) reales
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost

# Si usás Postgres en prod, poné tu URL:
# DATABASE_URL=postgres://usuario:pass@host:5432/tu_db
